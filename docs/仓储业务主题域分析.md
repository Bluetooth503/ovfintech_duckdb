# 仓储业务主题域分析

> 基于数据库抽样数据的业务架构梳理
>
> 分析时间: 2026-04-08
>
> 数据源: `/orchid_wms`

---

## 📊 仓储业务主题域划分

### 一、基础配置域 (Foundation)

管理仓库基础档案，为其他域提供基础支撑。

#### 核心实体

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `warehouse` | 仓库主表 | code, name, type, attribute, status, supervise_type |
| `warehouse_area` | 库区表 | warehouse_id, code, name, status |
| `warehouse_position` | 库位表 | warehouse_area_id, code, name, position_type |
| `warehouse_category` | 仓库品类表 | - |
| `warehouse_business_category` | 业务品类关联表 | - |
| `warehouse_ext` | 仓库扩展信息 | - |
| `warehouse_address` | 仓库地址表 | - |
| `warehouse_member` | 仓库成员表 | - |

#### 业务价值

- **仓库管理**: 区分平面仓/立体仓、自建库/第三方库
- **库区划分**: 按功能或商品特性划分存储区域
- **库位管理**: 精确到xyz坐标的货位定位
- **监管配置**: 静态监管、动态监管、非监管模式

---

### 二、入库域 (Inbound)

覆盖从预约到上架的完整入库流程。

#### 核心实体

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `reserve_inbound` | 入库预约单 | reserve_no, plan_date, warehouse_id, status |
| `reserve_inbound_detail` | 预约明细 | sku_id, plan_num, batch_no |
| `receipt_inbound` | 收货单 | serial_no, status, warehouse_id, provider_id |
| `receipt_inbound_detail` | 收货明细 | sku_id, receipt_num, batch_no, warehouse_position_id |
| `receipt_inbound_batch` | 收货批次 | batch_no, production_date, quality_days |
| `order_inbound` | 入库单 | inbound_no, order_type, storage_status, customer_id |
| `order_inbound_detail` | 入库明细 | sku_id, num, batch_no, warehouse_position_id |
| `order_inbound_history` | 入库历史 | - |
| `receipt_detail_bar_code` | 收货条码明细 | goods_code, batch_no, status |
| `bind_goods_bar_code` | 条码绑定记录 | - |

#### 业务流程

```
入库预约 → 车辆进场 → 收货卸货 → 质检取样 → 上架入库 → 库存更新
```

#### 关键指标

- 预约准时率、收货及时率
- 上架准确率、入库效率
- 质检合格率

---

### 三、出库域 (Outbound)

覆盖从通知到发货的完整出库流程。

#### 核心实体

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `outbound_notice` | 出库通知单 | pick_up_no, plan_pick_up_date, outbound_status |
| `outbound_notice_detail` | 通知明细 | sku_id, plan_num |
| `outbound_notice_car_record` | 通知车辆记录 | car_nos, driver |
| `outbound_pickup` | 拣货单 | pickup_no, status, warehouse_id |
| `outbound_pickup_detail` | 拣货明细 | sku_id, pickup_num, warehouse_position_id |
| `order_outbound` | 出库单 | outbound_no, order_type, storage_status, customer_id |
| `order_outbound_detail` | 出库明细 | sku_id, num, batch_no |
| `order_outbound_history` | 出库历史 | - |
| `outbound_load_review` | 装车复核 | status, review_result |
| `outbound_weighting` | 出库称重 | weight, tare_weight, net_weight |
| `outbound_detail_bar_code_record` | 出库条码记录 | goods_code, status |

#### 业务流程

```
出库通知 → 拣货分配 → 装车复核 → 称重确认 → 出库放行 → 库存扣减
```

#### 关键指标

- 拣货准确率、出库及时率
- 装车复核通过率
- 出库差错率

---

### 四、库存域 (Inventory)

库存主数据管理及变动追踪。

#### 核心实体

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `inventory` | 库存主表 | sku_id, batch_no, remain_weight_num, warehouse_position_id |
| `inventory_log` | 库存变动日志 | change_type, change_num, before_num, after_num |
| `inventory_operate_log` | 库存操作日志 | operate_type, operate_desc |
| `inventory_lock_record` | 库存锁定记录 | lock_type, lock_num, lock_reason |
| `inventory_seal` | 库存封签 | seal_no, status |
| `split_inventory_info` | 库存拆分主表 | - |
| `split_inventory_detail` | 库存拆分明细 | - |
| `cow_sync_inventory` | 库存同步记录 | - |
| `temp_inventory_statistic` | 库存临时统计 | - |

#### 关键概念

- **库存状态**: 可用库存、冻结库存、锁定库存
- **库存层次**: 总计件数量、总计价数量
- **锁定类型**: 订单锁定、内部锁定、质押锁定

#### 关键指标

- 库存准确率、库存周转率
- 库存差异率、呆滞库存占比

---

### 五、盘点域 (Inventory Check)

库存盘点计划与执行管理。

#### 核心实体

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `check_inventory` | 盘点单 | serial_no, check_type, check_method, check_scope, status |
| `check_inventory_detail` | 盘点明细 | sku_id, batch_no, sys_num, check_num, diff_num |
| `check_inventory_explain` | 盘点差异说明 | diff_reason, explain_result |
| `make_inventory` | 盘盈盘亏单 | make_type, make_num, status |
| `make_inventory_detail` | 盘盈盘亏明细 | - |
| `copper_inventory_check` | 铜库存盘点 | - |

#### 盘点方式

- **明盘/盲盘**: 是否显示系统库存
- **全盘/抽盘**: 盘点范围选择
- **按商品/批次/库位**: 盘点维度

---

### 六、过户调拨域 (Transfer)

库存所有权变更及仓库间调拨。

#### 核心实体

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `order_deliver` | 过户单 | deliver_no, old_goods_owner_id, new_goods_owner_id |
| `order_deliver_detail` | 过户明细 | sku_id, batch_no, deliver_num |
| `order_transfer` | 调拨单 | transfer_no, out_warehouse_id, in_warehouse_id |
| `order_transfer_detail` | 调拨明细 | sku_id, batch_no, transfer_num |

#### 业务类型

- **过户**: 货主变更，仓库不变
- **调拨**: 仓库变更，货主不变

---

### 七、计量称重域 (Weighing)

出入库计量与重量管理。

#### 核心实体

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `order_weight_record` | 称重记录 | gross_weight, tare_weight, net_weight, unit |
| `order_weight_record_temporary` | 临时称重记录 | - |
| `wms_weight_record_apply` | 称重申请 | apply_type, status |
| `wms_weight_record_apply_form` | 称重申请表单 | - |
| `wms_material_density` | 物料密度 | sku_id, density_value |

#### 称重类型

- **入库称重**: 毛重 - 皮重 = 净重
- **出库称重**: 确认出库数量
- **回皮称重**: 车辆空车称重

---

### 八、检测质检域 (Quality Inspection)

入库商品质量检测管理。

#### 核心实体

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `detect_info` | 检测单 | serial_no, detect_type, status, warehouse_id |
| `detect_detail` | 检测明细 | detect_item, detect_value, detect_result |
| `detect_property` | 检测属性 | property_name, property_code, property_type |
| `detect_report_property_mapping` | 检测报表属性映射 | - |
| `reserve_detect` | 预约检测 | - |
| `reserve_detect_goods` | 预约检测商品 | - |
| `reserve_detect_sample` | 预约检测样品 | - |

#### 检测类型

- **入库检测**: 商品入库前的质量检验
- **预约检测**: 提前预约的检测服务

---

### 九、监管金融域 (Supervision & Finance)

金融仓储核心业务，质押监管与融资服务。

#### 核心实体

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `pledge_release` | 质押/解押单 | pledge_no, type, plan_weight_num, status |
| `pledge_release_detail` | 质押明细 | sku_id, batch_no, pledge_num |
| `repay_lock_info` | 还款锁定信息 | lock_num, unlock_num |
| `supervision_report_info` | 监管日报 | report_date, check_charge, check_weight, patrol_record_count |
| `supervision_report_inventory_detail` | 监管日报库存明细 | - |
| `yy_loan_apply` | 融资申请 | apply_id, loan_status |
| `yy_loan_apply_detail` | 融资明细 | - |
| `factory_indicator_monitoring` | 工厂指标监控 | indicator_type, indicator_value, warning_threshold |
| `customer_indicator_warning` | 客户指标预警 | warning_type, warning_level |
| `customer_calculate_formula` | 客户计算公式 | formula_code, formula_content |

#### 业务模式

- **质押监管**: 以库存商品为质押物的金融服务
- **动态监管**: 允许库存变动但保持质押率
- **静态监管**: 质押期间库存冻结

#### 关键指标

- 质押率、解押率
- 监管日报准确率
- 风险预警及时率

---

### 十、园区车辆域 (Yard & Vehicle)

园区车辆进出与排队管理。

#### 核心实体

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `park_entry_register` | 进场登记 | register_no, number_plate, entry_type, register_status |
| `park_info` | 园区信息 | park_name, black_list_state, white_list_state |
| `park_car_sign` | 车辆签到 | car_no, sign_time, status |
| `park_car_reserve_queue` | 车辆预约排队 | reserve_no, queue_no, status |
| `park_queue_ticket` | 排队 ticket | ticket_no, queue_type |
| `park_doorway` | 园区道闸 | doorway_code, doorway_type |
| `park_doorway_goods` | 道闸商品关联 | - |
| `car_work_queue` | 车辆作业队列 | car_no, work_type, queue_status |
| `car_work_record` | 车辆作业记录 | start_time, end_time, work_result |
| `order_road_gate_record` | 道闸通行记录 | gate_type, pass_time, car_no |
| `park_rule_conf` | 园区规则配置 | rule_type, rule_content |
| `park_warehouse` | 园区仓库关联 | - |

#### 业务流程

```
车辆预约 → 进场登记 → 排队叫号 → 作业执行 → 离场确认
```

---

### 十一、监控预警域 (Monitoring & Alert)

仓库安防监控与风险预警。

#### 核心实体

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `monitor_area_info` | 监控区域 | area_code, area_name, area_type |
| `monitor_bind_area_info` | 区域绑定 | device_no, area_id |
| `monitor_scene_device` | 场景设备 | scene_type, device_no |
| `monitoring_plan_conf` | 监控计划配置 | plan_name, plan_type |
| `monitor_warn_msg_info` | 监控预警消息 | message_type, warn_time, status |
| `monitor_warn_msg_detail` | 预警明细 | warn_content, deal_result |
| `monitor_warn_msg_dtl` | 预警详情 | - |
| `warehouse_warn_msg_info` | 仓库预警消息 | - |
| `warehouse_warn_msg_detail` | 仓库预警明细 | - |
| `prophylactic_monitoring_conf` | 防控监控配置 | monitoring_type, threshold |
| `prophylactic_rule_conf` | 防控规则配置 | rule_code, rule_content |
| `ai_check_plan_conf` | AI检测计划配置 | - |
| `warehouse_patrol_record` | 仓库巡库记录 | patrol_date, patrol_status, patrol_exception |

#### 监控维度

- **视频监控**: 摄像头实时监控
- **安防报警**: 非法入侵、异常移动检测
- **设备监控**: 设备在线/离线状态
- **AI识别**: 智能分析与风险预警

---

### 十二、仓库设备域 (Warehouse Device)

仓库硬件设备全生命周期管理，是供应链金融风控的重要基础设施。

#### 核心实体

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `wms_device_info` | 设备信息 | device_name, device_type, device_code, device_status, net_status, work_status |
| `wms_device_area_mapping` | 设备区域映射 | device_id, area_id |
| `warehouse_device` | 仓库设备 | warehouse_id, device_type, status |
| `warehouse_position_device` | 库位设备 | position_id, device_id |
| `warehouse_provider_device` | 服务商设备 | provider_id, device_type |
| `device_binding` | 设备绑定 | device_id, bind_type, bind_status |
| `warehouse_monitor_scene` | 监控场景 | scene_name, scene_type |
| `warehouse_show_camera_config` | 摄像头展示配置 | - |

#### 设备类型

- **盘料仪**: 固定式/手持式库存盘点设备，用于质押物库存核实
- **摄像头**: 监控安防设备，实时监控质押物状态
- **道闸**: 车辆进出控制设备，管控质押物流动
- **称重设备**: 地磅、台秤等计量设备，精确计量质押物重量
- **传感器**: 温湿度、烟感等环境监控设备

#### 供应链金融风控价值

| 风控场景 | 设备作用 | 风险指标 |
|----------|----------|----------|
| **质押物真实性** | 盘料仪自动盘点，验证库存数据 | 盘点差异率、设备在线率 |
| **质押物安全** | 摄像头24小时监控，异常自动告警 | 监控覆盖率、告警响应时间 |
| **质押物流动** | 道闸控制车辆进出，防止私自出货 |  unauthorized出库拦截率 |
| **环境风险** | 温湿度传感器监控存储条件 | 环境异常告警次数 |
| **设备可靠性** | 心跳监测设备在线状态 | 设备离线时长、故障恢复时间 |

#### 设备状态与风控等级

```
设备正常 → 监控有效 → 质押物安全 → 风险可控
设备离线 → 监控盲区 → 质押物风险 → 触发预警 → 冻结额度
```

- **设备在线率**: 直接影响质押融资的可用额度
- **盘点准确率**: 决定质押物估值的可信度
- **监控覆盖率**: 影响风险预警的及时性

---

### 十三、单据表单域 (Documents)

电子单据与合同管理。

#### 核心实体

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `plan_form` | 计划表单 | plan_no, type, file_path, status |
| `warehouse_doc_config` | 仓库单据配置 | doc_type, template_id |
| `warehouse_doc_template` | 单据模板 | template_name, template_content |
| `invoice_order` | 发货单 | invoice_no, shipper_id, member_id, send_time |
| `invoice_detail` | 发货明细 | sku_id, num, batch_no |
| `invoice_bar_code` | 发货条码 | goods_code, status |
| `invoice_order_trace_info` | 发货追踪 | trace_node, trace_time |
| `material_deliver_detail` | 物料配送明细 | - |
| `order_contract` | 订单合同 | contract_no, contract_type |
| `external_order` | 外部订单 | external_no, source_system |

---

### 十四、临时辅助域 (Temporary & Auxiliary)

临时数据与系统辅助功能。

#### 核心实体

| 表名 | 说明 |
|------|------|
| `temp_excel_import` | Excel导入临时表 |
| `temp_goods_detail` | 商品明细临时表 |
| `temp_inventory_statistic` | 库存统计临时表 |
| `temp_task_statistics` | 任务统计临时表 |
| `temp_car_number` | 车牌号临时表 |
| `temp_deliver_goods_bar_code` | 发货条码临时表 |
| `temp_ocr_goods_bar_code` | OCR识别条码临时表 |
| `wms_operate_log` | 操作日志 |
| `error_node_log` | 错误节点日志 |
| `scan_task_log` | 扫描任务日志 |
| `scan_task_result_detail` | 扫描结果明细 |
| `inventory_report_detail` | 库存报表明细 |
| `rule` | 基础规则配置 |
| `association_agreement_conf` | 关联协议配置 |
| `product_favorite` | 商品收藏 |
| `wms_url_config` | URL配置 |
| `special_customer_calculation` | 特殊客户计算规则 |

---

## 🎯 核心业务闭环

### 入库闭环

```
入库预约 → 车辆进场 → 收货卸货 → 质检取样 → 上架入库 → 库存更新 → 监管上报
```

### 出库闭环

```
出库通知 → 库存锁定 → 拣货分配 → 装车复核 → 称重确认 → 出库放行 → 库存扣减
```

### 金融闭环

```
质押申请 → 库存冻结 → 放款确认 → 日常监管 → 解押申请 → 库存解冻 → 结清确认
```

### 监控闭环

```
AI监控 → 预警发现 → 工单派发 → 现场处理 → 结果反馈 → 巡检验证
```

---

## 📈 关键业务指标

| 类别 | 指标 | 说明 |
|------|------|------|
| **入库指标** | 预约准时率、收货及时率、上架准确率 | 入库效率 |
| **出库指标** | 拣货准确率、出库及时率、装车差错率 | 出库效率 |
| **库存指标** | 库存准确率、库存周转率、呆滞库存占比 | 库存健康 |
| **金融指标** | 质押率、监管合规率、风险预警率 | 金融风控 |
| **运营指标** | 设备在线率、预警处理及时率、盘点差异率 | 运营质量 |
| **设备风控指标** | 设备覆盖率、盘点准确率、监控有效性 | 金融风控 |

---

## 🔍 业务架构特点

### 1. 金融仓储模式

- **质押监管**: 以库存商品为质押物的供应链金融服务
- **动态监管**: 支持库存动态变动的监管模式
- **风险预警**: 价格监控、库存异常、设备离线等多维度风控

### 2. 物联网深度融合（设备即风控基础设施）

- **设备联网**: 盘料仪、摄像头、道闸、地磅等设备统一接入
  - **质押物盘点**: 盘料仪自动盘点，确保质押物账实相符
  - **实时监控**: 摄像头24小时监控质押物安全状态
  - **流动管控**: 道闸控制车辆进出，防止私自转移质押物
- **AI智能**: 智能识别、自动盘点、异常预警
- **数据同步**: 实时库存同步、设备状态监控
- **设备风控联动**: 设备离线自动触发风控预警，冻结融资额度

### 3. 精细化仓储管理

- **库位级管理**: 精确到xyz坐标的货位定位
- **批次追踪**: 完整的批次追溯体系
- **条码管理**: 一品一码，全程可追溯

### 4. 园区物流协同

- **车辆预约**: 提前预约，错峰到仓
- **排队调度**: 智能排队，公平高效
- **道闸联动**: 自动识别，快速通行

---

## 📋 与 dbt 项目映射建议

### 目录结构

按**数据仓库分层**规划目录，通过**模型命名**区分业务主题域。

```
models/
├── wms/                    # 仓储业务域
│   ├── ods/                # ODS层: 原始数据存储
│   ├── dim/                # DIM层: 维度表
│   ├── dwd/                # DWD层: 明细事实表
│   ├── dws/                # DWS层: 汇总表
│   └── ads/                # ADS层: 应用数据服务
├── common/                 # 公共维度表(跨业务域共享)
│   └── dim/
└── docs/                   # 单据表单域(如有需要)
```

### 命名规范

| 层级 | 命名格式 | 示例 |
|------|----------|------|
| ODS | `ods_{源表名}` | `ods_warehouse`, `ods_inventory` |
| DIM | `dim_{业务域}_{实体}` | `dim_wms_warehouse`, `dim_wms_position`, `dim_wms_sku` |
| DWD | `dwd_{业务域}_{主题}_{类型}_{周期}` | `dwd_wms_inbound_trx_i`, `dwd_wms_inventory_snap_1d_d` |
| DWS | `dws_{业务域}_{主题}_{类型}_{周期}` | `dws_wms_inbound_agg_1d_d`, `dws_wms_pledge_agg_1d_d` |
| ADS | `ads_{业务域}_{应用}_{周期}` | `ads_wms_inventory_dashboard_td`, `ads_wms_pledge_risk_td` |

**类型标识**: `trx`-事务明细, `snap`-快照, `agg`-聚合, `state`-状态

**周期标识**: `i`-增量, `f`-全量, `1d`-日, `1w`-周, `1m`-月, `td`-至今

### 业务主题域与模型映射

| 业务主题域 | ODS层 | DIM层 | DWD层 | DWS层 |
|------------|-------|-------|-------|-------|
| **基础配置** | `ods_warehouse` | `dim_wms_warehouse`<br>`dim_wms_area`<br>`dim_wms_position` | - | - |
| **入库** | `ods_order_inbound`<br>`ods_receipt_inbound` | `dim_wms_inbound_type` | `dwd_wms_inbound_trx_i` | `dws_wms_inbound_agg_1d_d` |
| **出库** | `ods_order_outbound`<br>`ods_outbound_notice` | `dim_wms_outbound_type` | `dwd_wms_outbound_trx_i` | `dws_wms_outbound_agg_1d_d` |
| **库存** | `ods_inventory`<br>`ods_inventory_log` | `dim_wms_sku`<br>`dim_wms_batch` | `dwd_wms_inventory_snap_1d_d`<br>`dwd_wms_inventory_trx_i` | `dws_wms_inventory_agg_1d_d`<br>`dws_wms_inventory_turnover_1d_d` |
| **盘点** | `ods_check_inventory` | - | `dwd_wms_check_trx_i` | `dws_wms_check_agg_1d_d` |
| **过户调拨** | `ods_order_transfer`<br>`ods_order_deliver` | - | `dwd_wms_transfer_trx_i` | `dws_wms_transfer_agg_1d_d` |
| **计量称重** | `ods_order_weight_record` | - | `dwd_wms_weighing_trx_i` | `dws_wms_weighing_agg_1d_d` |
| **检测质检** | `ods_detect_info` | `dim_wms_detect_item` | `dwd_wms_detect_trx_i` | `dws_wms_detect_agg_1d_d` |
| **监管金融** | `ods_pledge_release`<br>`ods_yy_loan_apply` | `dim_wms_pledge_type` | `dwd_wms_pledge_trx_i`<br>`dwd_wms_loan_trx_i` | `dws_wms_pledge_agg_1d_d`<br>`dws_wms_loan_risk_agg_1d_d` |
| **园区车辆** | `ods_park_entry_register`<br>`ods_car_work_record` | `dim_wms_park`<br>`dim_wms_vehicle` | `dwd_wms_vehicle_trx_i` | `dws_wms_yard_throughput_1d_d` |
| **监控预警** | `ods_monitor_warn_msg_info`<br>`ods_warehouse_patrol_record` | `dim_wms_warn_type` | `dwd_wms_patrol_trx_i`<br>`dwd_wms_alert_trx_i` | `dws_wms_alert_agg_1d_d` |
| **仓库设备** | `ods_wms_device_info` | `dim_wms_device_type` | `dwd_wms_device_status_trx_i`<br>`dwd_wms_device_check_trx_i` | `dws_wms_device_status_agg_1d_d`<br>`dws_wms_device_risk_agg_1d_d` |

### 模型示例

#### ODS层示例
```sql
-- models/wms/ods/ods_warehouse.sql
{{ config(materialized='table') }}

SELECT
    id AS warehouse_id,
    code AS warehouse_code,
    name AS warehouse_name,
    type AS warehouse_type,
    attribute AS warehouse_attribute,
    supervise_type,
    CURRENT_TIMESTAMP AS etl_time
FROM {{ source('orchid_wms', 'warehouse') }}
```

#### DIM层示例
```sql
-- models/wms/dim/dim_wms_warehouse.sql
{{ config(materialized='table') }}

SELECT
    warehouse_id,
    warehouse_code,
    warehouse_name,
    warehouse_type,
    supervise_type,
    CURRENT_TIMESTAMP AS dim_update_time
FROM {{ ref('ods_warehouse') }}
WHERE is_deleted = 0
```

#### DWD层示例
```sql
-- models/wms/dwd/dwd_wms_inbound_trx_i.sql
{{ config(materialized='incremental', unique_key='inbound_detail_id') }}

SELECT
    id AS inbound_detail_id,
    inbound_no,
    warehouse_id,
    customer_id,
    sku_id,
    num,
    batch_no,
    status,
    create_time,
    'orchid_wms' AS source_system
FROM {{ ref('ods_order_inbound_detail') }}
{% if is_incremental() %}
WHERE create_time > (SELECT MAX(create_time) FROM {{ this }})
{% endif %}
```

#### DWS层示例
```sql
-- models/wms/dws/dws_wms_inbound_agg_1d_d.sql
{{ config(materialized='table') }}

SELECT
    warehouse_id,
    sku_id,
    DATE(create_time) AS stat_date,
    COUNT(*) AS inbound_order_cnt,
    SUM(num) AS inbound_num,
    CURRENT_TIMESTAMP AS etl_time
FROM {{ ref('dwd_wms_inbound_trx_i') }}
GROUP BY warehouse_id, sku_id, DATE(create_time)
```

#### ADS层示例
```sql
-- models/wms/ads/ads_wms_inventory_dashboard_td.sql
{{ config(materialized='table') }}

SELECT
    warehouse_id,
    SUM(remain_weight_num) AS total_inventory,
    SUM(frozen_weight_num) AS frozen_inventory,
    SUM(lock_weight_num) AS locked_inventory,
    CURRENT_TIMESTAMP AS etl_time
FROM {{ ref('dws_wms_inventory_agg_1d_d') }}
WHERE stat_date = CURRENT_DATE
GROUP BY warehouse_id
```

---

## 🚀 数据仓库建设建议

### ODS 层 (models/wms/ods/)

- 直接加载业务表数据，保持原结构
- 统一 `org_id` AS `customer_id` 为租户隔离字段
- 添加 `etl_time` 数据抽取时间戳
- 命名: `ods_{源系统}_{源表名}`

| 模型 | 说明 |
|------|------|
| `ods_warehouse` | 仓库表 |
| `ods_warehouse_area` | 库区表 |
| `ods_warehouse_position` | 库位表 |
| `ods_inventory` | 库存表 |
| `ods_order_inbound` | 入库单表 |
| `ods_order_outbound` | 出库单表 |
| `ods_pledge_release` | 质押解押表 |
| `ods_wms_device_info` | 设备信息表 |

### DIM 层 (models/wms/dim/)

- 维度表，支持SCD Type 2缓慢变化维
- 命名: `dim_{业务域}_{实体}`

| 模型 | 说明 |
|------|------|
| `dim_wms_warehouse` | 仓库维度表 |
| `dim_wms_area` | 库区维度表 |
| `dim_wms_position` | 库位维度表 |
| `dim_wms_sku` | 商品SKU维度表 |
| `dim_wms_customer` | 客户维度表 |
| `dim_wms_device_type` | 设备类型维度表 |

### DWD 层 (models/wms/dwd/)

- 明细事实表，保留业务过程的原子数据
- 命名: `dwd_{业务域}_{主题}_{类型}_{周期}`

| 模型 | 类型 | 说明 |
|------|------|------|
| `dwd_wms_inbound_trx_i` | trx | 入库事务明细(增量) |
| `dwd_wms_outbound_trx_i` | trx | 出库事务明细(增量) |
| `dwd_wms_inventory_snap_1d_d` | snap | 库存日快照(日增量) |
| `dwd_wms_check_trx_i` | trx | 盘点事务明细(增量) |
| `dwd_wms_transfer_trx_i` | trx | 过户调拨事务(增量) |
| `dwd_wms_weighing_trx_i` | trx | 称重事务明细(增量) |
| `dwd_wms_detect_trx_i` | trx | 检测事务明细(增量) |
| `dwd_wms_pledge_trx_i` | trx | 质押解押事务(增量) |
| `dwd_wms_vehicle_trx_i` | trx | 车辆作业事务(增量) |
| `dwd_wms_patrol_trx_i` | trx | 巡库事务明细(增量) |
| `dwd_wms_device_status_trx_i` | trx | 设备状态变更(增量) |
| `dwd_wms_device_check_trx_i` | trx | 设备盘点事务(增量) |

### DWS 层 (models/wms/dws/)

- 汇总表，按业务主题进行聚合统计
- 命名: `dws_{业务域}_{主题}_{类型}_{周期}`

| 模型 | 类型 | 说明 |
|------|------|------|
| `dws_wms_inventory_agg_1d_d` | agg | 库存日统计 |
| `dws_wms_inventory_turnover_1d_d` | agg | 库存周转统计 |
| `dws_wms_inbound_agg_1d_d` | agg | 入库日统计 |
| `dws_wms_outbound_agg_1d_d` | agg | 出库日统计 |
| `dws_wms_pledge_agg_1d_d` | agg | 质押监管日统计 |
| `dws_wms_pledge_risk_agg_1d_d` | agg | 质押风险日统计 |
| `dws_wms_yard_throughput_1d_d` | agg | 园区吞吐量统计 |
| `dws_wms_device_status_agg_1d_d` | agg | 设备状态统计 |
| `dws_wms_device_risk_agg_1d_d` | agg | 设备风险统计 |
| `dws_wms_alert_agg_1d_d` | agg | 预警日统计 |

### ADS 层 (models/wms/ads/)

- 应用数据服务，面向具体业务场景
- 命名: `ads_{业务域}_{应用}_{周期}`

| 模型 | 说明 |
|------|------|
| `ads_wms_inventory_dashboard_td` | 库存看板(至今) |
| `ads_wms_inbound_outbound_analysis_1d_d` | 出入库分析(日度) |
| `ads_wms_pledge_risk_analysis_td` | 质押风险分析(至今) |
| `ads_wms_device_risk_control_td` | 设备风控分析(至今) |
| `ads_wms_pledge_inventory_verification_td` | 质押物盘点核实(至今) |
| `ads_wms_yard_efficiency_1d_d` | 园区效率分析(日度) |
| `ads_wms_finance_supervision_rpt_1d_d` | 金融监管日报 |
- `ads_wms_yard_efficiency_analysis_1d_d` - 园区效率分析(日度)
- `ads_wms_device_health_analysis_td` - 设备健康分析(至今)
- `ads_wms_device_risk_control_td` - 设备风控分析(至今)
- `ads_wms_pledge_inventory_verification_td` - 质押物盘点核实分析(至今)
- `ads_wms_finance_supervision_rpt_1d_d` - 金融监管日报

---

## 📝 总结

仓储业务是一个典型的**物联网 + 供应链金融 + 精细化运营**的复合型业务系统：

1. **资产数字化**: 从入库到出库的全流程条码追踪
2. **风控智能化**: AI监控 + 价格监控 + 库存监控 + 设备监控的多维风控体系
3. **金融场景化**: 基于库存商品的供应链金融产品
4. **运营精细化**: 库位级管理、批次级追踪、实时库存同步
5. **设备即风控**: 仓库设备是供应链金融风控的重要基础设施

### 仓库设备与供应链金融风控

在金融仓储场景中，**设备即风控能力**：

| 风控层级 | 设备能力 | 业务价值 |
|----------|----------|----------|
| **第一层：盘点核实** | 盘料仪自动盘点 | 确保质押物账实相符，防止虚假质押 |
| **第二层：实时监控** | 摄像头全覆盖 | 24小时监控质押物状态，及时发现异常 |
| **第三层：流动管控** | 道闸联动控制 | 防止未经授权的质押物转移 |
| **第四层：环境保障** | 温湿度传感器 | 确保质押物存储环境符合要求 |
| **第五层：状态感知** | 设备心跳监测 | 设备离线自动触发风控预警 |

### 数据仓库建设重点

- **库存流动性**分析(入库、在库、出库周转)
- **金融风险**监控(质押率、库存异常、价格风险、设备风险)
- **设备风控**能力(设备在线率、盘点准确率、监控覆盖率)
- **运营效率**提升(出入库效率、盘点准确率、设备利用率)
- **园区协同**优化(车辆周转、排队效率、道闸通行)

---

*本分析基于数据库抽样数据生成，仅供数据仓库建设参考*
